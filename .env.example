# Example env (no secrets). Copy to .env.local and fill values locally.
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_NAME=my_local_db
DB_SSL=false

# Example encoded DATABASE_URL (do not commit real secrets)
# DATABASE_URL=postgresql://postgres:your_password%40here@localhost:5432/my_local_db

# Frontend API base (replace with your backend domain when deployed)
# VITE_API_BASE_URL=/api/v1
