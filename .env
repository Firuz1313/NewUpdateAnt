# Local database configuration (placeholders)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
# DB_PASSWORD MUST NOT be committed. Set this locally (e.g., in .env.local) or in your deployment provider (Vercel).
# Use DevServerControl to set preview/dev env variables when testing in the cloud dev preview.
# Example (do NOT commit):
# DB_PASSWORD=anttechsupport@1313
DB_NAME=my_local_db
DB_SSL=false

# If you prefer a single connection string, set DATABASE_URL in your deployment provider.
# Example (URL-encode special characters such as @ -> %40):
# DATABASE_URL=postgresql://postgres:anttechsupport%401313@localhost:5432/my_local_db

# Vercel setup notes:
# - Add DATABASE_URL to Vercel Environment Variables (Production/Preview) with the real connection string.
# - Add frontend API base: VITE_API_BASE_URL=https://<your-backend-domain>/api/v1
# - Do NOT commit secrets to the repo. Use Vercel UI or `vercel env` to store secrets.
